doctype html
html
  head
    title Message Board

  body
    - if current_user
      - posts.each do |post|
        .post
        - if post.user == current_user
          .edit_post
            a href=edit_post_path(post)
              input.btn type="button" value="Edit"
        - if post.user == current_user
          .title data-title_id=post.id
            a href=post_path(post)
              = post.title
            .message data-message_id=post.id
              = post.user.name 
              |  
              ' wrote
              = post.message
        - else
          .title_without_edit data-title_id=post.id
            a href=post_path(post)
              = post.title
            .message data-message_id=post.id
              = current_user.name 
              |  
              ' wrote
              = post.message
          
        - if post.user == current_user
          .delete_post
            = button_to "Delete", post, :method => :delete, :confirm => 'Are you sure?'
	- else
	  .welcome_text
	    h3 Welcome to MessageBoard
	    br
	      h4 Sign in using
	    .signin_external
	      .signin_facebook
		    a href="/auth/facebook"
		      img.facebook src="/assets/facebook_64.png"
		    .facebook_label
		      | Facebook
		  .signin_twitter
		    a href="/auth/twitter"
		      img.twitter src="/assets/twitter_64.png"
		    .twitter_label
		      | Twitter
		  .signin_google
		    a href="/auth/google"
		      img.google src="/assets/google_64.png"
		    .google_label
		      | Google
		  .signin_github
		    a href="/auth/github"
		      img.github src="/assets/github_64.png"
		    .github_label
		      | Github
		  .signin_foursquare
		    a href="/auth/foursquare"
		      img.foursquare src="/assets/foursquare_64.jpg"
		    .foursquare_label
		      | Foursquare
		  .signin_linkedin
		    a href="/auth/linkedin"
		      img.linkedin src="/assets/linkedin_64.png"
		    .linkedin_label
		      | LinkedIn
		  .signin_tumblr
		    a href="/auth/tumblr"
		      img.tumblr src="/assets/tumblr_64.png"
		    .tumblr_label
		      | Tumblr